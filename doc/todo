* attention repertoire com/idl et idl/xsl
* faire statefull service
* faire un main service loader generique
* attention COMM versus COM
* remplacer la hierarchie  xtremweb.core.obj.ds.*; par  xtremweb.core.obj.*; pour pas avoir a se faire chier, mais attention au package.jdo !!!
* fair un API factory ou un truc dans le genre (api loader)
* changer le scheduler
	- actuellement une data a un attruid
	- il faut que un attribute ai une liste de data
	- comme ca le scheduler sera self contained (pas besoin de scanner le
	catalog)
* put some default value when creating poxwo objects 
* transformer le ActiveDataCallback en un EventHandler
* fixer le protocol.setserver et locator.set_drname
* virer TransferType vers dt
* APIFactory.createAPIs(comms, "BitDew", "ActiveData", "TransferManager");

* Documentation :
 repertoires doc qui ne doivent pas etre documente
 deplacer la mainpage
 faire les section Downloading (main), Configuring (conf), Using (cmdline), Programming (apis), Examples (examples)

